<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <permission
        android:name="com.ishdemon.ipc"
        android:protectionLevel="signature" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:name=".ServerApp"
        android:theme="@style/Theme.ClientServer">
        <activity
            android:name=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".SecureService"
            android:exported="true"
            android:permission="com.ishdemon.ipc">
            <intent-filter>
                <action android:name="AIDL_BIND" />
                <action android:name="MESSENGER_BIND" />
            </intent-filter>
        </service>
    </application>

</manifest>